# API Key for authentication with the tunnel server
API_KEY=your_secure_api_key_here

# Server URL (HTTP API endpoint for tunnel server)
API_SERVER_URL=https://your-tunnel-server.vercel.app

# Target API to forward requests to (internal API that's not directly accessible)
TARGET_API=http://localhost:8080

# Optional: Connection settings
# RECONNECT_INTERVAL=5000  # Reconnect interval in milliseconds
# HEARTBEAT_INTERVAL=25000  # Heartbeat interval in milliseconds
# POLL_INTERVAL=1000  # Polling retry interval in milliseconds 